[package]
name = "image_inspector"
version = "0.1.0"
edition = "2021"

[dependencies]

renderer = { path = "../renderer/" }

itertools = { workspace = true }

eframe = "0.30.0"
egui_plot = "0.30.0"
rfd = "0.15.2"
